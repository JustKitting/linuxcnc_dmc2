# Runtime and debug logs

Put persistent diagnostic output here instead of `/home/kit` or `/tmp`.
Log payloads are ignored by Git; this file only preserves the directory and
documents its responsibility.
