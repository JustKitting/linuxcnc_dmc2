# Firmware build outputs

Compiled Arduino images, compiler databases, and board-package caches belong
here. Firmware source remains under `firmware`; generated payloads are ignored
by Git and can be reproduced independently.
