# Firmware backups

Board readbacks and pre-change firmware backups belong here. The binary
payloads are retained locally and ignored by Git; source-controlled firmware
images intended for repeatable use live under `firmware/mesa/`.
